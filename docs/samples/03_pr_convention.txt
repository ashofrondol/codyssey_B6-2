$ python main.py pr --repo /tmp/tmp.Lrxn7P59ev --convention team --dry-run

[INFO] 저장소: /tmp/tmp.Lrxn7P59ev (브랜치 feature/retry-on-timeout)
[INFO] Git status 수집 완료: 3개 파일 변경 감지
[INFO] git diff HEAD 수집 완료: 69줄
[INFO] 컨벤션 'team' 적용 (/tmp/tmp.Lrxn7P59ev/.ai-gitgen.json)
[INFO] 안전 모드 ON — 마스킹 2건 · 파일 3/3개 · 줄 69/69줄 · 규칙별(anthropic-key×1, email×1)
[WARN] --dry-run: AI API 를 호출하지 않았습니다. 결과는 Git 사실만으로 조립한 초안입니다.
[WARN] 형식 위반 4건 — 위반 내용을 붙여 1회 재생성합니다.
       - 'Why' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'What' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'How to Test' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'Risk' 섹션의 불릿이 1개다. 최소 2개여야 한다.
[DONE] PR 초안 생성 완료 — API 호출 0회 (재생성 1회 포함)
[WARN] 후처리 4건을 적용했습니다.
       - 'Why' 섹션 불릿을 Git 사실로 1개 보충
       - 'What' 섹션 불릿을 Git 사실로 1개 보충
       - 'How to Test' 섹션 불릿을 Git 사실로 1개 보충
       - 'Risk' 섹션 불릿을 Git 사실로 1개 보충
[WARN] 규칙 위반 4건이 남아 후처리로 보정했습니다. 적용 전 검토하세요.
       - 'Why' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'What' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'How to Test' 섹션의 불릿이 1개다. 최소 2개여야 한다.
       - 'Risk' 섹션의 불릿이 1개다. 최소 2개여야 한다.

-------------------------- PR Title --------------------------
[DRY-RUN] feature/retry-on-timeout 브랜치 변경 사항
--------------------------------------------------------------

-------------------------- PR Body ---------------------------
## Why
- AI 호출 없이 만든 초안입니다. 실제 배경은 직접 채워야 합니다.
- 브랜치 feature/retry-on-timeout 의 변경 사항을 정리하기 위한 PR (배경은 작성자가 보완 필요)

## What
- 변경 파일 3개: .ai-gitgen.json, src/config.py, src/payment.py
- diff 69줄

## How to Test
- `git diff HEAD` 로 변경 내용을 확인
- 변경 파일 3개: .ai-gitgen.json, src/config.py, src/payment.py

## Risk
- (dry-run) risk 섹션은 실제 AI 호출에서 채워집니다
- 변경 파일 3개: .ai-gitgen.json, src/config.py, src/payment.py

## Checklist
- [ ] 로컬 테스트 통과
- [ ] 문서 갱신 확인
- [ ] 민감정보 미포함 확인
--------------------------------------------------------------
[INFO] 생성 결과는 초안입니다. 검토 후 직접 커밋/PR 에 적용하세요.
